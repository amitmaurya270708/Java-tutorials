# Escape Sequences
- Special character combinations in Java that start with a backslash ( \ ) followed by a specific character
- Used to represent characters that cannot be typed directly directly into a string, such as new lines, table, or quotation marks

# Common Escape Sequences in Java
## 1. Escaping Double Quotes
- If you want double quotes inside a string, you need to escape them with \"
 